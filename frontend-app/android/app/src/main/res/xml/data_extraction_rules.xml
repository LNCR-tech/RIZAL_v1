<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ data-extraction rules. Both flows (cloud backup and device-
  to-device transfer) are exclude="."  — Aura's encrypted token store and
  cached records never leave the device through Google's backup pipeline.

  Refs: https://developer.android.com/guide/topics/data/autobackup#XMLSyntax
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
